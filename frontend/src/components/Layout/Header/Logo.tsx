import React from "react";

type Props = {};

const Logo = (props: Props) => {
  return (
    <div>
      <img src="/images/logo.png" width={200} height={150} alt="logo" />
    </div>
  );
};

export default Logo;
